# Product brief

`<describe your app here>`

## Purpose

`<describe what the product does, who it is for, and what success looks like>`

## Stack (fill in)

| Layer | Choice |
|---|---|
| Backend | `<describe backend stack here>` |
| Frontend | `<describe frontend stack here>` |
| Local run | `<describe local runtime here — e.g. Docker Compose, uvicorn, Vite>` |
| Persistence | `<describe data ownership / store here>` |

## Boundaries

- `<describe what agents must not invent — e.g. cloud deploy, extra services>`
- `<describe design/safety rails agents must not silently bypass>`

## Entry points for agents

- Router: [`AGENTS.md`](AGENTS.md)
- Architecture / contracts: `docs/` (when present)
- Service READMEs: `api/README.md`, `frontend/README.md` (when present)
